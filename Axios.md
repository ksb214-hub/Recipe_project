# Axios

## 1. Axios 개요

Axios는 JavaScript에서 **HTTP 요청(API 호출)**을 쉽게 수행하기 위한 라이브러리이다.  
React 프론트엔드에서 **백엔드 서버(Spring Boot 등)와 통신하기 위해 사용된다.**

Axios를 사용하면 다음과 같은 작업을 쉽게 수행할 수 있다.

- REST API 호출
- 서버 데이터 요청 및 전송
- JSON 데이터 처리
- 인증 토큰 헤더 설정
- 비동기 HTTP 요청 처리

---
